---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <img src="/logo.svg" alt="Logo" class="logo"/>
    <header>
      <div class="web">
        <a href="/">Kacper Twardowski</a>
        <p>Web Developer & Graphic Designer</p>
      </div>
      <nav>
        <a href="/">Strona główna</a>
        <a href="/about/">O mnie</a>
        <a href="/portfolio/">Portfolio</a>
        <a href="/contact/">Kontakt</a>
      </nav>
    </header>
    <slot />
    <footer>
      <h3>-Kacper Twardowski | 2024-</h3>
      <ul>
        <li>
          <a href="https://www.linkedin.com/in/kacper-twardowski-a7b0a42a3/">Linkedin</a>
        </li>
        <li>
          <a href="https://www.instagram.com/_ktwardowski_/">Instagram</a>
        </li>
        <li>
          <a href="https://github.com/Panelinio">GitHub</a>
        </li>
        <li>
          <a href="mailto:kacper.tw150@gmail.com">Mail</a>
        </li>
      </ul>
    </footer>
  </body>
</html>
<style is:global>
  @import url("https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&family=Sono:wght@200..800&display=swap");
  :root {
    --accent: rgb(52, 103, 155);
    --accent-light: rgb(136, 187, 204);
    --accent-dark: rgb(7, 15, 43);
    --white: #f1f3e9;
    --dark: #20201f;
    --accent-gradient: linear-gradient(
      45deg,
      var(--accent),
      var(--accent-light) 30%,
      white 60%
    );
  }
  html {
    font-family: "Josefin Sans", sans-serif;
    background-color: var(--dark);
    color: var(--white);
    scroll-behavior: smooth;
  }

  footer {
    margin-top: auto;
    padding: 40px;
    letter-spacing: 2px;
    text-align: center;
    background-color: var(--accent);
    font-size: 1.5rem;
  }
  

  footer a {
    display: block;
    margin-top: 5px;
    color: var(--white);
    text-decoration: none;
    font-size: 1rem;
    margin: 0.5rem;
  }

  footer ul {
    list-style: none;
    padding: 0;
    margin: 10px 0 0 0;
    display: flex;
    justify-content: center;
  }

  footer ul li {
    margin: 0 10px;
  }

  .logo {
    width: 80px;
    height: 80px;
    position: absolute;
  }

  body {
    margin: 0;
    padding: 0;
  }

  main {
    margin: auto;
    margin-bottom: 1.4rem;
    width: 100%;
    font-size: 20px;
    line-height: 1.6;
    min-height: 95vh;
  }

  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6rem;
    width: 75%;
    margin: 0 auto;
    top: 0;
    background-color: var(--dark);
    z-index: 1000;
    box-shadow: inset 0 -1px 0 0 var(--accent);
  }

  header nav a {
    color: var(--white);
    text-decoration: none;
    position: relative;
    display: inline-block;
  }

  header nav a::after {
    content: '';
    position: absolute;
    width: 0;
    height: 2px;
    display: block;
    margin-top: 2px;
    left: 0;
    background: var(--accent);
    transition: width 0.4s ease, left 0.4s ease;
  }

  header nav a:hover::after {
    width: 100%;
    left: 0;
  }

  .web {
    display: flex;
    flex-direction: column;
    padding: 1rem 1rem 0rem 0rem;
    color: var(--accent);
  }

  .web a {
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--accent);
    text-decoration: none;
  }

  .web p {
    font-size: 1.2rem;
    font-weight: 300;
    margin-top: 5px;
    color: #f1f3e9;
  }

  nav {
    display: flex;
    justify-content: center;
    font-size: 1.4rem;
  }

  nav a {
    margin-left: 2.5rem;
    transition: all 0.2s ease-out 0.5ms;
  }

  nav a:hover {
    color: var(--accent);
  }

  p {
    font-size: 1.6rem;
    font-weight: 300;
    max-width: 65ch;
    text-wrap: balance;
  }
  .landing {
     display: flex; 
     padding-top: 6rem;
     margin: 0rem 12rem 0rem 12rem;
  }
  .left {
	width: 40%;
	padding-right: 1rem;
  }
  .right {
	width: 60%;
  }
  span, strong {
	color: var(--accent);
  }
  
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
  }

  .text-gradient {
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }

  /* ===== Scrollbar CSS ===== */
  /* Firefox */
  * {
    scrollbar-width: auto;
    scrollbar-color: var(--accent-light) var(--dark);
  }

  /* Chrome, Edge, and Safari */
  *::-webkit-scrollbar {
    width: 16px;
  }

  *::-webkit-scrollbar-track {
    background: var(--dark);
  }

  *::-webkit-scrollbar-thumb {
    background-color: var(--accent-light);
  }
</style>
